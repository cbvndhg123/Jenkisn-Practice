#include<bits/stdc++.h>
using namespace std;
vector<int>arr={1,2,3,4,5}

vector<int>result;

for(int i=k;i<arr.size();i++)
    result.push_back(arr[i]);

for(int i=0;i<k;i++)
    result.push_back(arr[i]);
